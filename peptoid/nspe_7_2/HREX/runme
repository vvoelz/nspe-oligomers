module load gromacs/2021.2

# Production Run to see how much ns can system run per day
gmx grompp -f prod.mdp -c input.gro -p peptoid.top -o prod.tpr

# make an index file 
echo "\!27\nname 28 peptoid\nq\n" | gmx make_ndx -f 19AE1-4-A_protonated_ion.gro -o index.ndx
echo "q" | gmx make_ndx -n index.ndx

# Transfer the file name
# Prepare n state with different FEP lambda value
#i prepare_tpr.sh
# clean all the state folder:

rm -r state*

# Activate the prepare_tpr.sh and run
chmod +x prepare_tpr.sh
./prepare_tpr.sh
# The result should be state_n/ include all the file and tpr for later running

# Submit the qsub job
qsub fep.qsub  

# Copy all the dhdl use for analyze
chmod +x analyze_dhdl.sh
./analyze_dhdl.sh

# For continued HREX, change the prepare_tpr.sh file 
mv HREMD.part0001.gro 19AE1-4-A_fep.gro

# And exame the correctness using:

diff state_0/19AE1-4-A_fep.gro ../HREX_1/state_0/HREMD.part0001.gro

